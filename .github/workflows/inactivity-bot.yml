name: inactivity bot

on:
  schedule:
    - cron: "0 0 * * *"

  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    permissions:
      issues: write

    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log("Bot started");
